import { Banner } from '../components/banner';

const Homepage= ({recordatorio}) => {
    return(
        <div>
        <Banner recordatorio={recordatorio}/>
        </div>
    )
}

export {Homepage}
